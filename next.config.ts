// Favicon config for Next.js
// No explicit config needed for favicon.svg in /public, but you can add a <link> in _app or layout
import type { NextConfig } from "next";

const nextConfig: NextConfig = {};

export default nextConfig;
